[changelog]
header = "# Changelog\n"
body = """
{% if version -%}
## [{{ version | trim_start_matches(pat="v") }}](https://github.com/alexeev-prog/nadzoring/releases/tag/{{version}}) - {{ timestamp | date(format="%Y-%m-%d") }}
{% else -%}
## [Unreleased]
{% endif -%}

{% for group, commits in commits | group_by(attribute="group") %}
### {{ group | upper_first }}
{% for commit in commits %}
- {{ commit.message | trim_start_matches(pat="^\\w+:{1,2}\\s*") | trim_start_matches(pat="^\\w+\\s*") | upper_first }}
  {%- if commit.pr_number %} ([#{{ commit.pr_number }}](https://github.com/alexeev-prog/nadzoring/pull/{{ commit.pr_number }})){% endif -%}
  {%- if commit.breaking %} [breaking]{% endif -%}
  ([{{ commit.id | truncate(length=7, end="") }}](https://github.com/alexeev-prog/nadzoring/commit/{{ commit.id }}))
{%- endfor %}
{% endfor %}
"""
footer = "<!-- generated by git-cliff -->"

[git]
conventional_commits = false
filter_unconventional = false

# Skip merge commits from being processed individually
[[git.commit_parsers]]
message = "^Merge pull request"
skip = true

[[git.commit_parsers]]
message = "^Merge branch"
skip = true

[[git.commit_parsers]]
message = "^Merge remote-tracking branch"
skip = true

[[git.commit_parsers]]
message = "^Merge tag"
skip = true

# Breaking changes (must come first)
[[git.commit_parsers]]
message = ".*!:.*"
group = "⚠️ Breaking Changes"

# Features
[[git.commit_parsers]]
message = "^feat(\\(.*\\))?:?"
group = "Features"

[[git.commit_parsers]]
message = "^feature(\\(.*\\))?:?"
group = "Features"

[[git.commit_parsers]]
message = "^add(\\(.*\\))?:?"
group = "Features"

[[git.commit_parsers]]
message = "^implement(\\(.*\\))?:?"
group = "Features"

[[git.commit_parsers]]
message = "^create(\\(.*\\))?:?"
group = "Features"

# Bug Fixes
[[git.commit_parsers]]
message = "^fix(\\(.*\\))?:?"
group = "Bug Fixes"

[[git.commit_parsers]]
message = "^hotfix(\\(.*\\))?:?"
group = "Bug Fixes"

[[git.commit_parsers]]
message = "^bugfix(\\(.*\\))?:?"
group = "Bug Fixes"

[[git.commit_parsers]]
message = "^correct(\\(.*\\))?:?"
group = "Bug Fixes"

[[git.commit_parsers]]
message = "^resolve(\\(.*\\))?:?"
group = "Bug Fixes"

# Documentation
[[git.commit_parsers]]
message = "^docs(\\(.*\\))?:?"
group = "Documentation"

[[git.commit_parsers]]
message = "^documentation(\\(.*\\))?:?"
group = "Documentation"

[[git.commit_parsers]]
message = "^readme(\\(.*\\))?:?"
group = "Documentation"

# Performance
[[git.commit_parsers]]
message = "^perf(\\(.*\\))?:?"
group = "Performance Improvements"

[[git.commit_parsers]]
message = "^performance(\\(.*\\))?:?"
group = "Performance Improvements"

[[git.commit_parsers]]
message = "^optimize(\\(.*\\))?:?"
group = "Performance Improvements"

# Refactoring
[[git.commit_parsers]]
message = "^refactor(\\(.*\\))?:?"
group = "Code Refactoring"

[[git.commit_parsers]]
message = "^rework(\\(.*\\))?:?"
group = "Code Refactoring"

# Testing
[[git.commit_parsers]]
message = "^test(\\(.*\\))?:?"
group = "Testing"

[[git.commit_parsers]]
message = "^tests(\\(.*\\))?:?"
group = "Testing"

# Code Style
[[git.commit_parsers]]
message = "^style(\\(.*\\))?:?"
group = "Code Style"

[[git.commit_parsers]]
message = "^format(\\(.*\\))?:?"
group = "Code Style"

# CI/CD
[[git.commit_parsers]]
message = "^ci(\\(.*\\))?:?"
group = "CI/CD"

[[git.commit_parsers]]
message = "^workflow(\\(.*\\))?:?"
group = "CI/CD"

[[git.commit_parsers]]
message = "^actions(\\(.*\\))?:?"
group = "CI/CD"

# Dependencies
[[git.commit_parsers]]
message = "^deps(\\(.*\\))?:?"
group = "Dependencies"

[[git.commit_parsers]]
message = "^dependency(\\(.*\\))?:?"
group = "Dependencies"

[[git.commit_parsers]]
message = "^requirements(\\(.*\\))?:?"
group = "Dependencies"

[[git.commit_parsers]]
message = "^poetry(\\(.*\\))?:?"
group = "Dependencies"

[[git.commit_parsers]]
message = "^pip(\\(.*\\))?:?"
group = "Dependencies"

[[git.commit_parsers]]
message = "^bump(\\(.*\\))?:?"
group = "Dependencies"

# Build System
[[git.commit_parsers]]
message = "^build(\\(.*\\))?:?"
group = "Build System"

[[git.commit_parsers]]
message = "^pyproject(\\(.*\\))?:?"
group = "Build System"

[[git.commit_parsers]]
message = "^setup(\\(.*\\))?:?"
group = "Build System"

# Maintenance
[[git.commit_parsers]]
message = "^chore(\\(.*\\))?:?"
group = "Maintenance"

[[git.commit_parsers]]
message = "^maintenance(\\(.*\\))?:?"
group = "Maintenance"

[[git.commit_parsers]]
message = "^cleanup(\\(.*\\))?:?"
group = "Maintenance"

# Security
[[git.commit_parsers]]
message = "^security(\\(.*\\))?:?"
group = "Security"

[[git.commit_parsers]]
message = "^sec(\\(.*\\))?:?"
group = "Security"

# Typo fixes
[[git.commit_parsers]]
message = "^typo(\\(.*\\))?:?"
group = "Typo Fixes"

[[git.commit_parsers]]
message = "^spelling(\\(.*\\))?:?"
group = "Typo Fixes"

# Reverts
[[git.commit_parsers]]
message = "^revert(\\(.*\\))?:?"
group = "Reverts"

# Version bumps (skip these)
[[git.commit_parsers]]
message = "^bump version"
skip = true

[[git.commit_parsers]]
message = "^release:"
skip = true

[[git.commit_parsers]]
message = "^chore\\(release\\)"
skip = true

# Timestamp auto-commits (skip these)
[[git.commit_parsers]]
message = "^Update \\d{4}-\\d{2}-\\d{2} \\d{2}:\\d{2}:\\d{2}$"
skip = true

[[git.commit_parsers]]
message = "^\\[skip ci\\] Update \\d{4}-\\d{2}-\\d{2} \\d{2}:\\d{2}:\\d{2}$"
skip = true

# Co-authored-by commits from bots (skip these)
[[git.commit_parsers]]
message = "^Update .*Co-authored-by:.*devin-ai-integration.*$"
skip = true

# Catch-all for conventional commits format
[[git.commit_parsers]]
message = "^(\\w+)(?:\\(([^)]+)\\))?!?:\\s+(.+)"
group = "Other Changes"

# Everything else goes to Other Changes
[[git.commit_parsers]]
message = ".*"
group = "Other Changes"
